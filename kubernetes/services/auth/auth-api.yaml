apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
  namespace: eshop-microservices
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-api
  template:
    metadata:
      labels:
        app: auth-api
    spec:
      containers:
      - name: auth-api
        image: ${REGISTRY:-}authapi:${TAG:-latest}
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ConnectionStrings__Database
          value: "Host=auth-postgres;Database=AuthDb;Username=auth_user;Password=Auth_P@ssw0rd"
        - name: ConnectionStrings__Redis
          value: "basket-redis:6379"
        - name: EventBusSettings__HostAddress
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: EventBusSettings__HostAddress
        - name: JwtSettings__Key
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: JwtSettings__Key
        - name: JwtSettings__Issuer
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: JwtSettings__Issuer
        - name: JwtSettings__Audience
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: JwtSettings__Audience
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: eshop-microservices
spec:
  selector:
    app: auth-api
  ports:
  - port: 80
    targetPort: 80